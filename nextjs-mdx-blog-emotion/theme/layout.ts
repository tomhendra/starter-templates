import { spacing } from './sizing';
import { shadows } from './shadows';

export const layout = {
  header: {
    background: 'inherit',
    boxShadow: shadows.sm,
  },
  main: {
    paddingTop: spacing[8],
  },
  footer: {
    background: 'inherit',
  },
};
